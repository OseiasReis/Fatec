#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) 
{
  float base, larg, area;
  printf("Digite o valor da base:", base);
  scanf ("%d",&base);
  printf("Digite o valor da largura:", larg);
  scanf ("%d",&larg);
  area = base*larg;
  media = (prim + segu + terc + quar)/4; 
  printf("Sua media é de: %d", media);
  getch();
  system("PAUSE");	
  return 0;
}
incompleto
