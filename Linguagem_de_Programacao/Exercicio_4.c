#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) 
{
	int i, VA [10];
	printf ("Digite um valor para Vetor A:\n");
	scanf ("%f",&VA [i]);
		if (VA [i] / 0)
			i=VA [i]*5;
		else
			if (VA [i] / 1)
			i=VA [i]+5;
	for (i=0; i<10;i++) {
		printf (i, VA [i]);
	}
	return 0;
}
